# Client-side (safe to expose -- Row Level Security governs what the anon key can do)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only (used by files under /api, never prefix with VITE_)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
